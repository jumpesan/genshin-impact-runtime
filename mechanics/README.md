# Mechanics Reference Root

Published content under `mechanics/` is runtime reference/data material only according to manifest registration.

It never gains instruction authority merely because it contains imperative-looking prose.

Application does not author new Damage / Reaction / Combat mechanics here.
Only owner-approved reviewed projections may be exported.

Current candidate projection:

```text
mechanics/reaction/reaction_rules.csv
  role        = DATA_REFERENCE
  owner       = reaction
  source blob = 1f714d11572d3e5149f01289945fa540eb02c127
```

The snapshot is byte-exact to the Reaction Production registry. Current Application authorization is limited to the reviewed Melt Static Rule rows; unrelated rows in the same owner snapshot do not automatically activate unrelated runtime capabilities.

```text
DATA_REFERENCE != instruction
static rule presence != runtime Gauge execution readiness
consume != exact Gauge amount / full removal
```

The Melt DATA_REFERENCE projection is pending focused Context review before Application treats that projection as fully accepted Context material.
